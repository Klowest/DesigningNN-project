# DesigningNN-project — Microservices Edition

## Проект выполнили:

Фронин Иван
Гумеров Ильсур
Андрей Киселев
Пичугов Виктор

![Демонстрация](Bars_Real_cut.gif)

Проект **DesigningNN-project** демонстрирует микросервисную архитектуру для разработки и развертывания приложения, состоящего из нескольких сервисов (frontend, backend, model). Каждый сервис является отдельным контейнером Docker, а их взаимодействие осуществляется через **Docker Compose**.

---

## Описание

Этот проект демонстрирует, как можно создать и развёртывать приложение с использованием микросервисной архитектуры. Он разделяет приложение на несколько компонентов:

- **frontend**: Интерфейс пользователя для взаимодействия с системой.
- **backend**: API-сервис для обработки запросов.
- **model**: Логика, связанная с обработкой данных, машинным обучением или вычислениями.

Проект включает в себя все необходимые настройки для локального запуска с помощью Docker.

---

## Стек технологий

- **Frontend**: React (или другой фреймворк, используемый для создания пользовательского интерфейса)
- **Backend**: Node.js, Express (или другой веб-сервер/фреймворк для обработки API)
- **Model**: Python (или другой язык/библиотека для реализации модели)
- **Docker**: Для контейнеризации сервисов
- **Docker Compose**: Для организации взаимодействия между сервисами

---

## Функциональные возможности

- Разделение приложения на независимые микросервисы.
- Лёгкое взаимодействие между сервисами через API.
- Возможность масштабирования каждого компонента приложения.
- Интеграция с модельной логикой для выполнения вычислений.

---

## Системные требования

Для успешного запуска проекта у вас должны быть установлены следующие компоненты:

- [Docker](https://www.docker.com/)
- [Docker Compose](https://docs.docker.com/compose/)
- [Git](https://git-scm.com/)

---

## Как запустить проект

### 1. Клонирование репозитория

Сначала клонируйте репозиторий:

```bash
git clone https://github.com/Klowest/DesigningNN-project.git
cd DesigningNN-project
git checkout microservices

### 2. Запуск проекта

Для запуска проекта с помощью Docker Compose выполните команду:

```bash
docker-compose up --build

Эта команда выполнит сборку и запуск всех сервисов, указанных в файле docker-compose.yaml.

### 3. Открытие приложения
После того как контейнеры будут запущены, откройте браузер и перейдите по следующему адресу:

http://localhost:3000

Убедитесь, что порт в docker-compose.yaml совпадает с тем, который указан в настройках.

## Структура проекта
DesigningNN-project/
│
├── backend/             # API-сервис
│   └── [Исходники backend сервиса]
│
├── frontend/            # Пользовательский интерфейс
│   └── [Исходники frontend сервиса]
│
├── model/               # Логика модели/вычислений
│   └── [Исходники модели]
│
├── docker-compose.yaml  # Конфигурация для запуска всех сервисов
└── README.md            # Этот файл
